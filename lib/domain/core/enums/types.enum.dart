enum EventCategory { lecture, church, other }

enum EventType {
  vision,
  pillar,
  foundational,
  tutorial,
  live,
  experience,
  encounter,
  assembly,
  other,
}

enum ScanType { scanIn, scanOut }

enum EventReportType { full, late, absent, lateOrAbsent }
